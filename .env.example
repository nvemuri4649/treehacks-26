#── Local Nemotron (vLLM on DGX Spark) ─────────────────────────────────
#Endpoint for the vLLM server running Nemotron on the DGX Spark.
#Start vLLM with:  vllm serve nvidia/NVIDIA-Nemotron-Nano-9B-v2 --port 8001 --trust-remote-code
#all local computation (agent, encryption, etc.) runs on this machine.
NEMOTRON_ENDPOINT=http://spark-abcd.local:8001/v1
NEMOTRON_MODEL=nvidia/NVIDIA-Nemotron-Nano-9B-v2

#── Cloud API Keys ────────────────────────────────────────────────────
#Anthropic API Key (required for Claude cloud models via Claude Agent SDK)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

#OpenAI API Key (required for GPT cloud models)
OPENAI_API_KEY=sk-your-openai-key-here

#── Cloud model default ──────────────────────────────────────────────
#Which cloud model to use: "claude-sonnet-4-20250514", "claude-haiku-4-20250414", "gpt-4o", "gpt-4o-mini"
DEFAULT_CLOUD_MODEL=claude-sonnet-4-20250514

#encryption server (likeness protection on GPU backend)
ENCRYPTION_SERVER_URL=http://spark-abcd.local:5000
ENCRYPTION_DEFAULT_ITERS=200

#── Server ────────────────────────────────────────────────────────────
HOST=127.0.0.1
PORT=8000
